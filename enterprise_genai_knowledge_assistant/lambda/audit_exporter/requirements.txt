# boto3 is provided by AWS Lambda runtime, no need to include it
opensearch-py==2.3.1
aws-requests-auth==0.4.3
requests-aws4auth==1.2.3
# tiktoken is optional - code has fallback if not available
# tiktoken==0.5.2

